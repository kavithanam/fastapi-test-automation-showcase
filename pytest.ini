[pytest]
markers =
    contract: contract tests that validate cross-service response assumptions
    integration: tests that require both services running locally
    e2e: end-to-end user journey tests across services
    ui: selenium browser tests for frontend behavior
